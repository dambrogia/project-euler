/**
 * @param mixed n
 * @returns Number
 */
const execute = n => {
    return false;
};

test('test base case problem', () => {
    expect(execute(1)).toBe(true);
});

test('test use case problem', () => {
    expect(execute(1).toBe(true);
});
